{% extends '_skeletons/_list.html.twig' %}
{% set entity = "inputs" %}
{%  block title %}Entrées de marchandises {% endblock %}
        {% block show %}
            {#<a href="{{ path(entity ~ '_show', {'slug' : object.id}) }}" class="ui blue circular tiny icon button">#}
                {#<i class="zoom icon"></i></a>#}
            {#</a>#}
        {% endblock %}
        {% block update %}
            {#<a href="{{ path(entity ~ '_update', {'slug' : object.id}) }}" class="ui olive circular tiny icon button">#}
                {#<i class="edit icon"></i>#}
            {#</a>#}
        {% endblock %}
        {% block delete %}
            {#<a href="{{ path(entity ~ '_delete', {'slug' : object.id}) }}" class="ui red circular tiny icon button">#}
                {#<i class="delete icon"></i>#}
            {#</a>#}
        {% endblock %}

{% block element_title %}
        {#<a href="{{ path(entity ~ '_show', {'slug' : object.id}) }}">{{ object.id }}</a>#}
{% endblock %}
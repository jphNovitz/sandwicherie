{# receive a list and display each element #}
<ul class="list">
    {% for object in objects|slice(0, 5) %}
        <a href="{{ path(object|entity~'_show', {'id': object.id}) }}"><li>{{ object.name }}</li></a>
    {% endfor  %}
</ul>
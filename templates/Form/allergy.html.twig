{%  extends 'base.html.twig' %}

{% block body %}
    <h1>Allergy Form</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">GO</button>
    {{ form_end(form) }}
{% endblock %}